# Normalize line endings to LF on commit, checkout as LF on all OSes
* text=auto eol=lf

# Force binary for images and fonts so git never mangles them
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
